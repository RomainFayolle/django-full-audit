============
Installation
============

At the command line::

    $ easy_install django_full_audit

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_full_audit
    $ pip install django_full_audit
